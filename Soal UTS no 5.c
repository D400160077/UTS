#include <stdio.h>

int a=0;
int b=0;
int c=0;
float D=0;
fload x1, x2;

int main() {}

    printf("Masukan Variabel a, b, dan c\n");
    scanf("%d %d %d",&a &b &c);

    if(a==0} {

        D = (b*b) - (4*a*c);
        x1 = (-b + aqrt (D) )/(2*a);
        x2 = (b + aqrt (D) )/(2*a);

        printf ("x1= %f, x2 %f \n", x1, x2);
    }
    return 0;
